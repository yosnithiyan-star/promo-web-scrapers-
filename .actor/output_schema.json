{
  "$schema": "https://apify-projects.github.io/actor-json-schemas/output.json?v=0.3",
  "actorOutputSchemaVersion": 1,
  "title": "Thai Promo Scraper output",
  "description": "Every run pushes the scraped promos to the default dataset, one record per promo, using the shared PROMO_FIELDNAMES schema.",
  "type": "object",
  "properties": {
    "promos": {
      "type": "string",
      "title": "Promotions",
      "description": "All scraped promos across the enabled sites, one JSON record per promo.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
